# ⚠️ АНАЛІЗ ЛІМІТАЦІЙ

## Що перевірив

Проаналізував заплановані фічі на предмет технічних обмежень, потенційних проблем та того, що може знадобитися додатково.

---

## ✅ ПРАЦЮВАТИМЕ БЕЗ ПРОБЛЕМ

### 1. Базовий лендінг (HTML/CSS/JS)
- **Статус:** ✅ Повністю працює
- **Обмеження:** Ніяких
- Claude Code створить без проблем

### 2. Інтерактивне демо з кнопками
- **Статус:** ✅ Працює
- **Обмеження:** Ніяких
- Захардкоджений JSON з фільмами — стандартний JS

### 3. Лайв-стрічка (фейкова)
- **Статус:** ✅ Працює
- **Обмеження:** Ніяких
- setInterval + рандом — базовий JS

### 4. Темна/світла тема
- **Статус:** ✅ Працює
- **Обмеження:** Ніяких
- CSS variables + JS для перемикання

### 5. Контекст часу доби
- **Статус:** ✅ Працює
- **Обмеження:** Визначає час браузера користувача (може відрізнятись від реального)
- **Рішення:** Не критично, бо час браузера = час користувача

### 6. FAQ акордеон
- **Статус:** ✅ Працює
- **Обмеження:** Ніяких
- Чистий CSS (details/summary) або JS

### 7. UTM-мітки
- **Статус:** ✅ Працює
- **Обмеження:** Ніяких
- Звичайні URL-параметри

---

## ⚠️ ПОТРЕБУЄ УВАГИ

### 1. localStorage для збережених фільмів

**Статус:** ⚠️ Працює з нюансами

**Обмеження:**
- Не працює в приватному режимі Safari
- Очищається при "Clear browsing data"
- Не синхронізується між пристроями

**Рішення:**
- Додати try/catch для Safari private mode
- Попередити користувача що список локальний
- Не критично для MVP

**Код для обробки:**
```javascript
function safeLocalStorage(key, value) {
  try {
    if (value === undefined) {
      return JSON.parse(localStorage.getItem(key) || '[]');
    }
    localStorage.setItem(key, JSON.stringify(value));
    return true;
  } catch (e) {
    console.warn('localStorage недоступний');
    return value === undefined ? [] : false;
  }
}
```

---

### 2. Deep link з назвами фільмів у Telegram

**Статус:** ⚠️ Є обмеження

**Обмеження:**
- Telegram start parameter: максимум 64 символи
- Можна передати тільки латиницю, цифри, _
- Не можна передати "Круелла, Дюна, Інтерстеллар" напряму

**Рішення:**
```javascript
// Варіант 1: Передавати ID фільмів
// saved_1_5_12 замість назв
window.location.href = `https://t.me/BOT?start=saved_${movieIds.join('_')}`;

// Варіант 2: Короткі коди
// Бот декодує: cr=Круелла, du=Дюна
window.location.href = `https://t.me/BOT?start=s_cr_du_in`;

// Варіант 3: Зберігати на сервері (потребує backend)
// Генеруємо унікальний код, бот запитує сервер
```

**Рекомендація:** Варіант 1 або 2 — не потребує сервера

---

### 3. Постери фільмів

**Статус:** ⚠️ Потребує рішення

**Обмеження:**
- Якщо використовувати реальні постери — потрібні права / API
- TMDB API безкоштовний, але потребує ключа
- Завантаження зображень збільшує час загрузки

**Варіанти:**
| Варіант | Плюси | Мінуси |
|---------|-------|--------|
| Без постерів | Просто, швидко | Менш привабливо |
| Emoji як постери | Креативно, швидко | Несерйозно |
| Захардкоджені постери | Контроль | Потрібно 50+ картинок |
| TMDB API | Реальні постери | Потребує API ключ |
| Placeholder з назвою | Стильно | Нестандартно |

**Рекомендація для MVP:** 
- Стилізований placeholder з назвою + рейтингом
- Або мінімум постерів (10-15 популярних фільмів)

**Приклад placeholder:**
```css
.movie-poster-placeholder {
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 48px;
  color: white;
  aspect-ratio: 2/3;
  border-radius: 8px;
}
/* Показуємо emoji жанру: 😂🔪💕🎲 */
```

---

### 4. Google Analytics 4

**Статус:** ⚠️ Потребує налаштування

**Що потрібно:**
1. Створити акаунт GA4 (безкоштовно)
2. Отримати Measurement ID (G-XXXXXXXXXX)
3. Налаштувати події в інтерфейсі GA4

**Обмеження:**
- Дані з'являються з затримкою 24-48 годин
- Блокується AdBlock (частина користувачів не трекається)

**Альтернатива:** 
- Plausible або Umami (простіше, privacy-friendly)
- Але потребують хостингу або платної підписки

---

### 5. Шрифти

**Статус:** ⚠️ Важливо для дизайну

**За замовчуванням Claude Code може використати:**
- Inter, Roboto (занадто generic)
- System fonts (не виділяється)

**Рекомендація:** Вказати конкретні шрифти

**Гарні варіанти для українського тексту:**
```css
/* Google Fonts - безкоштовно */
@import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&family=Open+Sans:wght@400;600&display=swap');

/* Або */
@import url('https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700&family=Roboto+Slab:wght@400;700&display=swap');

body {
  font-family: 'Open Sans', sans-serif;
}

h1, h2, h3 {
  font-family: 'Montserrat', sans-serif;
}
```

---

### 6. Хостинг

**Статус:** ⚠️ Потребує вибору

**Безкоштовні варіанти:**
| Платформа | Плюси | Мінуси |
|-----------|-------|--------|
| **Vercel** | Швидко, просто, HTTPS | Потрібен GitHub |
| **Netlify** | Швидко, форми | Потрібен GitHub |
| **GitHub Pages** | Безкоштовно | Тільки статика |
| **Cloudflare Pages** | Дуже швидко | Складніше налаштування |

**Рекомендація:** Vercel або Netlify — деплой за 5 хвилин

---

## ❌ НЕ ПРАЦЮВАТИМЕ БЕЗ BACKEND

### 1. Реальна статистика "підібрано фільмів"
- **Потребує:** Базу даних + API
- **Рішення MVP:** Фейкова статистика (норм для старту)

### 2. Синхронізація збережених фільмів між пристроями
- **Потребує:** Авторизацію + базу даних
- **Рішення MVP:** localStorage (тільки один пристрій)

### 3. Персоналізація на основі історії
- **Потребує:** Backend з ML
- **Рішення MVP:** Рандом по категоріях

### 4. A/B тестування заголовків
- **Потребує:** Сервіс типу Optimizely або свій backend
- **Рішення MVP:** Вибрати один варіант, тестувати пізніше

---

## 📋 ФІНАЛЬНИЙ ЧЕК-ЛИСТ ПЕРЕД РОЗРОБКОЮ

### Підготувати заздалегідь:

- [ ] **GA4 Measurement ID** — створити акаунт на analytics.google.com
- [ ] **Telegram Bot** — створити через @BotFather, отримати username
- [ ] **Telegram Channel** — створити, отримати username  
- [ ] **Домен** — купити або використати безкоштовний (yourname.vercel.app)
- [ ] **OG-картинка** — 1200x630px для шерінгу (можна згенерувати в Canva)
- [ ] **База фільмів** — підготувати 50-60 фільмів (15 на категорію × 4 категорії)

### Формат бази фільмів:

```javascript
const movies = {
  comedy: [
    {
      id: "cr2021",
      title: "Круелла",
      titleEn: "Cruella", // для пошуку постера
      year: 2021,
      rating: 7.4,
      genre: "Комедія, драма",
      duration: 134,
      description: "Стильно, дотепно, з Еммою Стоун",
      emoji: "😈" // для placeholder
    },
    // ... ще 14 комедій
  ],
  thriller: [/* 15 трилерів */],
  romance: [/* 15 романтичних */],
  random: [/* 15 різних */]
};
```

---

## 🚀 РЕКОМЕНДОВАНИЙ ПОРЯДОК РОЗРОБКИ

1. **Базовий лендінг** без інтерактиву (перевірити що деплоїться)
2. **Інтерактивне демо** з 5-10 фільмами (перевірити логіку)
3. **Стилізація** (теми, анімації, шрифти)
4. **Лайв-стрічка + лічильник** (соціальний доказ)
5. **localStorage збереження** (з fallback для Safari)
6. **GA4 + UTM** (аналітика)
7. **Тестування на мобільних** (критично!)
8. **Деплой + фінальна перевірка**

---

## 💡 БОНУС: Якщо хочеш постери без API

Створи placeholder-картки з градієнтами по жанрах:

```css
.poster-comedy { background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%); }
.poster-thriller { background: linear-gradient(135deg, #4a4a4a 0%, #1a1a1a 100%); }
.poster-romance { background: linear-gradient(135deg, #ff9a9e 0%, #fecfef 100%); }
.poster-random { background: linear-gradient(135deg, #a8edea 0%, #fed6e3 100%); }
```

Всередині — велике emoji жанру + назва фільму. Виглядає стильно і не потребує зовнішніх ресурсів.

---

*Документ готовий. Всі критичні обмеження враховані, є рішення для кожного.*
